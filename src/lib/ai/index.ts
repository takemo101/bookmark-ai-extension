/**
 * `ai/*` boundary.
 *
 * Owns Chrome Built-in AI / Prompt API availability checks, prompting, response
 * parsing, and Japanese AI output (description, genre, tags). It returns parsed
 * analysis results or typed errors and must not persist data directly. No
 * external AI API / API-key fallback in the MVP.
 *
 * Scaffold placeholder; real analysis lands in later issues (see
 * docs/design.md and docs/implementation-principles.md).
 */
export {}
