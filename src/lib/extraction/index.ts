/**
 * `extraction/*` boundary.
 *
 * Owns current-page extraction and structured excerpt construction (capped
 * around 8k-12k characters). It must never persist raw page excerpts.
 *
 * Scaffold placeholder; real extraction lands in later issues (see
 * docs/design.md and docs/implementation-principles.md).
 */
export {}
