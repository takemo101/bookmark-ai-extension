/**
 * `bookmarks/*` boundary.
 *
 * Owns the bookmark domain: JSONL schema, parse/serialize, URL canonicalization,
 * the first-class bookmark collection (upsert, delete, merge, search, filter),
 * and AI-status transitions. Pure logic — no Chrome, Drive, or Prompt API I/O.
 *
 * Scaffold placeholder; concrete model lands in later issues (see
 * docs/design.md and docs/implementation-principles.md).
 */
export {}
