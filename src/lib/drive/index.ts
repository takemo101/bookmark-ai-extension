/**
 * `drive/*` boundary.
 *
 * Owns Google auth token use, Drive folder/file bootstrap, metadata/revision
 * reads, download, and upload. I/O-focused: it must not embed bookmark-domain
 * decisions such as duplicate handling or AI status transitions.
 *
 * Scaffold placeholder; real Drive sync lands in later issues (see
 * docs/design.md and docs/implementation-principles.md).
 */
export {}
