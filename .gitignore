# asem runtime state (token-bearing files; never commit)
.asem/sessions/
.asem/current-session*.json
.asem/tokens/

# dependencies and build output
node_modules/
dist/

# local env — never commit real OAuth client IDs, secrets, or tokens
.env.local
.env.*.local

# local agent state (not product code)
.omc/
